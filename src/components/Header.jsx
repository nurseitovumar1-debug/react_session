export default function Header() {
  return (
    <header className="header">
      <img src="src/fotos/logo (1).svg" alt="" />
      <nav className="nav">
        <a href="#">About us</a>
        <button className="btn">Let's Talk →</button>
      </nav>
    </header>
  );
}
